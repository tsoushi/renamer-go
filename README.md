# help
```
   --format string, -f string  リネーム後のファイル名のフォーマットを指定する (default: "{year}{month}{day}_{index}.{ext}")
   --dry                       実際にはリネームを行わず、リネーム内容のみを表示する
   --interactive               リネーム前に確認を行う
   --help, -h                  show help
```
